import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    display: "flex",
    flexDirection: "row",
    width: 110,
    justifyContent: "space-between",
  },
  call_icon: {
    marginLeft: 7,
  },
});
